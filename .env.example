# Copiá a `.env` en la raíz del proyecto. Valores en Supabase → Project Settings → API.

VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Mismo UUID que el club insertado en supabase/migrations/..._initial_schema.sql
VITE_CLUB_ID=11111111-1111-1111-1111-111111111111

# Login por «usuario»: Supabase guarda email como usuario@DOMINIO. Mismo valor al crear usuarios (ej. admin@mi-club.auth).
# Si no lo definís, se usa club.auth (usuario admin → admin@club.auth).
# VITE_AUTH_EMAIL_DOMAIN=mi-club.auth

# Opcional: para `npm run import-supabase` (recomendado: service_role; bypass RLS).
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=
# SUPABASE_CLUB_ID=

# Opcional: nombre del archivo en public/ (ej. mi-club.png). Por defecto logo.png
# VITE_APP_LOGO=logo.png
